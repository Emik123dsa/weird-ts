import { AbstractStorage } from "./abstract-storage.interface";
/**
 * localStorage expression
 *
 * @export
 * @abstract
 * @class localStorage
 * @extends {AbstractStorage}
 */
export abstract class localStorage extends AbstractStorage { }; 
